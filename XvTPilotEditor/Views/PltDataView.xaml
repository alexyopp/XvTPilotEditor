<UserControl x:Class="XvTPilotEditor.Views.PltDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XvTPilotEditor.Views"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Pilot Name:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PilotName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Total Score:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding TotalScore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Player ID:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PlayerID, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>


            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Continued / Reflown Mission:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding ContinuedOrReflownMission, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Is Hosting:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding IsHosting, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Num Human Players In Mission:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding NumHumanPlayersInMission, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Front Fly Mode:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding FrontFlyMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- TODO -->
            <!-- Arrays as expanders (simple indexed list) -->

            <Expander Header="Unknown0x166" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding Unknown0x166}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Unknown0x186" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding Unknown0x186}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>


            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Promo Points:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PromoPoints, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Worse Promo Points:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding WorsePromoPoints, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Rank Adjustment Applied:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding RankAdjustmentApplied, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Percent To Next Rank:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding PercentToNextRank, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- TODO -->
            <!-- Mission category records (shown as expandable content) -->
            <Expander Header="Total Category Score" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalCategoryScore}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Num Flown Non-Series" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding NumFlownNonSeries}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Num Flown Series" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding NumFlownSeries}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Kills" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalKills}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Friendly Kills" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding FriendlyKills}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Full Kills On Player Rating" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalFullKillsOnPlayerRating}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Shared Kills On Player Rating" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalSharedKillsOnPlayerRating}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Assists On Player Rating" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalAssistsOnPlayerRating}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Full Kills On AI Rank" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalFullKillsOnAIRank}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Shared Kills On AI Rank" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalSharedKillsOnAIRank}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Assists On AI Rank" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalAssistsOnAIRank}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Hidden Cargo Found" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalHiddenCargoFound}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Laser Hit" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLaserHit}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Laser Fired" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLaserFired}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Warhead Hit" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalWarheadHit}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Warhead Fired" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalWarheadFired}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Craft Losses" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalCraftLosses}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Losses From Collisions" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLossesFromCollisions}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Losses From Starships" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLossesFromStarships}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Losses From Mines" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLossesFromMines}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Losses From Player Rank" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLossesFromPlayerRank}" TextWrapping="Wrap"/>
            </Expander>

            <Expander Header="Total Losses From AI Rank" IsExpanded="False" Margin="0,6,0,6">
                <TextBlock Text="{Binding TotalLossesFromAIRank}" TextWrapping="Wrap"/>
            </Expander>
            
            <!-- More scalar / array items -->
            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Current Rank:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding CurrentRank, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Total Count Missions Flown:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding TotalCountMissionsFlown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Rank String:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding RankString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Debrief Mission Score:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding DebriefMissionScore, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <!-- TODO -->
            <!-- Debrief arrays -->
            <Expander Header="Debrief Full Kills On Player" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefFullKillsOnPlayer}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Shared Kills On Player" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefSharedKillsOnPlayer}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Full Kills On FG" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefFullKillsOnFG}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Shared Kills On FG" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefSharedKillsOnFG}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Full Kills By Player" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefFullKillsByPlayer}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Shared Kills By Player" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefSharedKillsByPlayer}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Full Kills By FG" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefFullKillsByFG}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Shared Kills By FG" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefSharedKillsByFG}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Melee AI Rank FG" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefMeleeAIRankFG}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4">
                                <TextBlock Width="40" Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat='[{0}]'}"/>
                                <TextBox Width="200" Margin="8,0,0,0" Text="{Binding ., Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>
            
            <!-- Connected players and team results -->
            <Expander Header="Connected Players" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding ConnectedPlayer}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <Expander Header="Debrief Team Result" IsExpanded="False" Margin="0,6,0,6">
                <ItemsControl ItemsSource="{Binding DebriefTeamResult}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                <Label Content="Last Selected Faction:" VerticalAlignment="Center"/>
                <TextBox Text="{Binding LastSelectedFaction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            
        </StackPanel>
    </ScrollViewer>
</UserControl>
