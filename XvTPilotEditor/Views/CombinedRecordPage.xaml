<UserControl x:Class="XvTPilotEditor.Views.CombinedRecordPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XvTPilotEditor"
             xmlns:view="clr-namespace:XvTPilotEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Content="PltName:"               Grid.Row="0" Grid.Column="0"/>
        <TextBox                                Grid.Row="0" Grid.Column="1" Text="{Binding PltPilotName}"/>

        <Label Content="Pl2Name:"               Grid.Row="0" Grid.Column="2"/>
        <TextBox                                Grid.Row="0" Grid.Column="3" Text="{Binding Pl2PilotName}"/>

        <Label Content="PltTotalScore:"         Grid.Row="1" Grid.Column="0"/>
        <TextBox                                Grid.Row="1" Grid.Column="1" Text="{Binding PltTotalScore}"/>
                                                          
        <Label Content="Pl2TotalScore:"         Grid.Row="1" Grid.Column="2"/>
        <TextBox                                Grid.Row="1" Grid.Column="3" Text="{Binding Pl2TotalScore}"/>
    </Grid>
</UserControl>
